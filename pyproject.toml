
[project]
name = "flask_experiments"
description = "Just a playground"
license = {file = "LICENSE"}
readme = "README.md"
version = "0.1.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["api"]
